Вариант работы 9.

Чтобы привести в читабельный вид данные, воспользуемся «nfdump -r nfcapd.202002251200 -o csv > dump.csv»
Для запуска необходимо наличие в одной папке программы (main.py) и таблицы с данными (dump.csv).
Для корректной работы программы необходимо наличие Python на компьютере пользователя.

Контакты: krsnvnk@gmail.com